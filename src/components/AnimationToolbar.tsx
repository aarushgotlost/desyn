
"use client";

// This is a placeholder for a potentially more generic, shared animation toolbar
// that might be used in different contexts (e.g., a simplified editor preview).
// The main toolbar for the workspace is currently at /src/app/animation/[projectId]/toolbar.tsx

export default function AnimationToolbar() {
  return (
    <div className="p-2 bg-gray-200 border-b">
      <p className="text-sm text-gray-600">Shared Animation Toolbar Component</p>
      {/* Add generic controls or tool selectors here */}
    </div>
  );
}
